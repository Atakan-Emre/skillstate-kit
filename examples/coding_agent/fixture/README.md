# timebox fixture

A small Python package for duration conversion. The existing public API is
`timebox.format_seconds(seconds)`. Preserve its behavior. Tests use pytest.
The fixture has no network services or production data.
